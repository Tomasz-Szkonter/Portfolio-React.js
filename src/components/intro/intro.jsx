import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  background-color: red;
`;

const Intro = () => <Wrapper id="intro">INTRO</Wrapper>;

export default Intro;
