import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  background-color: green;
`;

const Portfolio = () => <Wrapper id="portfolio">Portfolio</Wrapper>;

export default Portfolio;
